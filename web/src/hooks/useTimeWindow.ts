// Compatibility export for older imports. Time-window state has one owner:
// TimeWindowContext, which resolves its defaults from the active League Workspace.
export { useTimeWindow } from '../contexts/TimeWindowContext';

{
  "version": 2,
  "buildCommand": "npm install && cd server && npm install --include=dev && npm run build && cd ../web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    {
      "source": "/api/coach/:path*",
      "destination": "/api/simple-test"
    },
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}

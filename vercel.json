{
  "version": 2,
  "buildCommand": "npm --prefix workstation run build",
  "outputDirectory": "workstation/dist",
  "installCommand": "npm --prefix workstation install",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://cracked-ice-web.vercel.app/api/:path*"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
